export interface ResourceResult {
  uri: string;
  label?: string;
  description?: string;
  type?: string;
}

export interface OntologyItem {
  uri: string;
  label: string;
  description: string;
}
